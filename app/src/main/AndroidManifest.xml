<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rwmol.cst2335_final_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".ActivityTrackerActivity" />
        <activity android:name=".HouseThermostatActivity" />
        <activity android:name=".NutritionTrackerActivity" />
        <activity android:name=".ActivityAddActivity" />
        <activity android:name=".ActivityViewHistory" />
        <activity android:name=".ActivityDetails" />
        <activity android:name=".ActivityStats" />

    </application>

</manifest>